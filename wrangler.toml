name = "k5m-fashion"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# MASTER branch — bound to the production DB (a61d4250, the seeded live data).
# The staging branch points at k5m-fashion-staging-db (7452fae8-...). Keep these
# divergent; on merge staging→master, always keep master's database_id. See DEPLOY.md.
[[d1_databases]]
binding = "DB"
database_name = "k5m-fashion-votes"
database_id = "a61d4250-9551-467c-8c2d-0860092c68c9"
